# =============================================================================
# Runtime dependencies — Interview Copilot API
#
# Install:  pip install -r requirements.txt
#
# Versions are pinned. To upgrade safely later, use pip-tools or uv.
# =============================================================================

# --- Web framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0          # standard extras pull in watchfiles, websockets, httptools

# --- Database (async Postgres + vector search) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0                    # async Postgres driver
alembic==1.13.3                    # schema migrations
pgvector==0.3.6                    # Python adapter for the pgvector extension (Day 4 RAG)

# --- Validation & config ---
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0             # required by Pydantic's EmailStr

# --- Auth ---
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4
bcrypt==4.0.1            # password hashing

# --- File uploads & PDF parsing ---
python-multipart==0.0.12           # FastAPI multipart form support (Day 3 resume upload)
pypdf==5.1.0                       # PDF text extraction

# --- AI providers ---
openai==1.54.4                     # generation + embeddings (Day 4-5)
anthropic==0.39.0                  # evaluation pipeline (Day 7)
tiktoken==0.8.0                    # token-aware chunking before embedding

# --- HTTP client ---
httpx==0.27.2                      # outbound HTTP; also used in tests

# --- Resilience ---
tenacity==9.0.0                    # retry decorator for flaky LLM API calls (NFR/TNFR-4)

# --- Observability (lightweight) ---
structlog==24.4.0                  # structured JSON logging (TNFR-3)
